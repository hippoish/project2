<!-- form to create a new post -->
<%= form_for [@neighborhood, @post] do |f| %>

  <div class="field">
    <%= f.label 'New Post' %><br />
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit "Post" %>
  </div>

<% end %>
